name: GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Determine version and publish URL
        id: version
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Get latest release tag via GitHub API, fall back to "dev"
          VERSION=$(gh release view --json tagName -q .tagName 2>/dev/null || echo "dev")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Using version: $VERSION"

          # Construct GitHub Pages URL
          REPO_NAME="${GITHUB_REPOSITORY#*/}"
          PUBLISH_URL="https://${GITHUB_REPOSITORY_OWNER}.github.io/${REPO_NAME}"
          echo "publish_url=$PUBLISH_URL" >> $GITHUB_OUTPUT
          echo "Publish URL: $PUBLISH_URL"

      - name: Build ESP Web Tools site
        run: |
          uvx ewt-gen music-dashboard.yaml \
            --title "Music Dashboard" \
            --publish-url "${{ steps.version.outputs.publish_url }}" \
            --fw-version "${{ steps.version.outputs.version }}" \
            -o _site/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
